# Milestone 05 – Model Context Protocol

## Objective
Expose capabilities via MCP servers so external tools like editors can orchestrate your workflows.

## Checklist
- Wrap retrieval and agent functions as MCP tools with clear input/output contracts.
- Connect with MCP-aware clients (VS Code, Cursor, Zed) to validate the integration.
- Document authentication, safety, and interoperability considerations.
